#import "mathblock.typ": mathblock

#mathblock[
  $
    (L dot L')^R & = (L')^R dot L^R \
                 \
               L & = {a_1, a_2, ..., a_m } "und" \
              L' & = {b_1, b_2, ..., b_m } \
        L dot L' & = {a_1, ... , a_m } dot {b_1, ..., b_m } \
                 & = {a_1 b_1, ..., a_m b_m } \
                 \
    (L dot L')^R & = {a_1 b_1, ..., a_m b_m}^R \
                 & = {b_1 a_1, ..., b_m a_m} \
                 & = {b_1, ..., b_m} dot {a_1, ..., a_m} \
                 &= (L')^R dot L^R #h(290pt) square
  $
]
